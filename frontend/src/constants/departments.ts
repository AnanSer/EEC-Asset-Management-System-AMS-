/**
 * EEC organizational department placeholders.
 * Real department data will be fetched from the API in Phase 2.
 */

export interface Department {
  id: string;
  name: string;
  code: string;
}

/** Placeholder — will be replaced by API data in Phase 2. */
export const DEPARTMENTS: Department[] = [];
